# Define entrypoint which relies on three library projects
project_root
suppress_rule: "naming_functions"

entrypoint "example.m" {
    libraries {
        "my_library",
        "common",
        "kitten"
    }
}
